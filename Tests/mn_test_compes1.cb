
<- testing compose method->
compose(p);
int inc;
note a = (C, 3, whole);
note b = (D, 3, whole);
note c = (F, 3, whole);
inc = 9;
chord c1 = ([(Db,4,quarter), (C#, 2, half)], half);

stanza p = [a, b, c, c1];

score m = [p];

compose(p);