scale s = [(A,0,1),(B,0,2),(C,0,3),(D,0,4),(E,0,5)];

int check = 0;

foreach (note n in s)
    check += 1;
    if((n.duration) is check)
        print(0);
    else
        print(3111);
    end
end

if(check is 5)
    print(0);
else
    print(3112);
end